// Auto-generated by branding generator. Do not edit directly.
// Source: tools/branding/templates/docker-bake.hcl.tmpl

// Base/control images (targets are configured at runtime via bake overrides).
variable "REGISTRY" {
  default = ""
}

variable "HOME" {
  default = ""
}

target "lambda-base" {
  context    = "cli/internal/infra/build/assets"
  dockerfile = "Dockerfile.lambda-base"
  tags       = ["${REGISTRY}{{SLUG}}-lambda-base:latest"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/base"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/base,mode=max"]
  output     = ["type=image,push=true"]
}

target "os-base" {
  context    = "services/common"
  dockerfile = "Dockerfile.os-base"
  tags = ["${REGISTRY}{{SLUG}}-os-base:latest"]
  args = {
    ROOT_CA_MOUNT_ID = "root_ca"
  }
  output = ["type=image,push=true"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/base"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/base,mode=max"]
  secret = ["id=root_ca,src=/home/akira/{{HOME_DIR}}/certs/rootCA.crt"]
}

target "python-base" {
  context    = "services/common"
  dockerfile = "Dockerfile.python-base"
  tags = ["${REGISTRY}{{SLUG}}-python-base:latest"]
  args = {
    ROOT_CA_MOUNT_ID = "root_ca"
  }
  output = ["type=image,push=true"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/base"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/base,mode=max"]
  secret = ["id=root_ca,src=/home/akira/{{HOME_DIR}}/certs/rootCA.crt"]
}

group "base-images" {
  targets = ["lambda-base", "os-base", "python-base"]
}

// Control plane images (Docker mode).
target "gateway-docker" {
  context    = "services/gateway"
  dockerfile = "Dockerfile.docker"
  contexts = {
    config      = "services/gateway/config"
    common      = "services/common"
    python-base = "target:python-base"
  }
  args = {
    PYTHON_BASE_IMAGE = "python-base"
    SERVICE_USER      = "{{SLUG}}"
    SERVICE_UID       = "1000"
    SERVICE_GID       = "1000"
  }
  tags   = ["${REGISTRY}{{SLUG}}-gateway-docker:latest"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/services"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/services,mode=max"]
  output = ["type=image,push=true"]
}

target "agent-docker" {
  context    = "services/agent"
  dockerfile = "Dockerfile.docker"
  contexts = {
    meta_module = "meta"
    os-base     = "target:os-base"
  }
  args = {
    OS_BASE_IMAGE = "os-base"
  }
  tags   = ["${REGISTRY}{{SLUG}}-agent-docker:latest"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/services"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/services,mode=max"]
  output = ["type=image,push=true"]
}

// Control plane images (shared/provisioner).
target "provisioner" {
  context    = "services/provisioner"
  dockerfile = "Dockerfile"
  contexts = {
    config           = "services/gateway/config"
    generator_assets = "cli/internal/infra/build/assets/site-packages"
    python-base      = "target:python-base"
  }
  args = {
    PYTHON_BASE_IMAGE = "python-base"
  }
  tags   = ["${REGISTRY}{{SLUG}}-provisioner:latest"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/services"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/services,mode=max"]
  output = ["type=image,push=true"]
}

// Control plane images (containerd mode).
target "runtime-node-containerd" {
  context    = "services/runtime-node"
  dockerfile = "Dockerfile.containerd"
  contexts = {
    os-base = "target:os-base"
  }
  args = {
    OS_BASE_IMAGE = "os-base"
  }
  tags   = ["${REGISTRY}{{SLUG}}-runtime-node-containerd:latest"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/services"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/services,mode=max"]
  output = ["type=image,push=true"]
}

target "agent-containerd" {
  context    = "services/agent"
  dockerfile = "Dockerfile.containerd"
  contexts = {
    meta_module = "meta"
    os-base     = "target:os-base"
  }
  args = {
    OS_BASE_IMAGE = "os-base"
  }
  tags   = ["${REGISTRY}{{SLUG}}-agent-containerd:latest"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/services"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/services,mode=max"]
  output = ["type=image,push=true"]
}

target "gateway-containerd" {
  context    = "services/gateway"
  dockerfile = "Dockerfile.containerd"
  contexts = {
    config      = "services/gateway/config"
    common      = "services/common"
    python-base = "target:python-base"
  }
  args = {
    PYTHON_BASE_IMAGE = "python-base"
    SERVICE_USER      = "{{SLUG}}"
    SERVICE_UID       = "1000"
    SERVICE_GID       = "1000"
  }
  tags   = ["${REGISTRY}{{SLUG}}-gateway-containerd:latest"]
  cache-from = ["type=local,src={{OUTPUT_DIR}}/buildx-cache/services"]
  cache-to   = ["type=local,dest={{OUTPUT_DIR}}/buildx-cache/services,mode=max"]
  output = ["type=image,push=true"]
}

group "control-images-docker" {
  targets = ["gateway-docker", "agent-docker", "provisioner"]
}

group "control-images-containerd" {
  targets = ["runtime-node-containerd", "gateway-containerd", "agent-containerd", "provisioner"]
}

group "default" {
  targets = ["base-images", "control-images-docker", "control-images-containerd"]
}
